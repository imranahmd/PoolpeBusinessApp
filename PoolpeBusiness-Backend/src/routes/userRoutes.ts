// import express, { Router,Request, Response } from "express";
// const UserController = require("../controllers/UserController");
// const router = Router();
// import { checkJwt } from "../middlewares/checkJwt";
//
// router.post("/all", [checkJwt],  UserController.listAll);
//
// router.post("/allotheruser", [checkJwt], UserController.listAllOtherUser);
// // router.get( "one/:id([0-9]+)", UserController.getOneById );
//
//
// router.get( "/user", [checkJwt], UserController.getOneById);
// router.post( "/edit", [checkJwt], UserController.editUser);
//
//
// router.post("/profilepicupload", [checkJwt], UserController.profilepicupload);
//
// export default router;